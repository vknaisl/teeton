println("Write a string:")
name = scan_string
length = len(name)
half_length = length / 2
i = 0

while(i < half_length) {
	tmp = get(name i)
	set(name i get(name (length - 1 - i)))
	set(name (length - 1 -i) tmp)
	i = i + 1
}
println(name)
